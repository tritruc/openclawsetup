# 2026-02-27

- Bật kênh `zalouser` trong OpenClaw và cài `zca` CLI (`0.0.24`).
- Trạng thái hiện tại: Zalo Personal channel ON nhưng chưa authenticated.
- Phát hiện `zca` yêu cầu license key trước khi gửi tin (`zca license support-code` -> `zca license activate <key>`).
- Đã tạo app web `apps/zalo-reminder-manager` (port mặc định `8799`) để quản lý:
  - tài khoản gửi (account/profile)
  - người nhận (kèm số điện thoại + threadId)
  - reminder CRUD
  - scheduler 8:00 + retry 30 phút đến khi nhận xác nhận ngày đúng định dạng.
- Đã thêm launcher `scripts/run_zalo_reminder_manager.sh`.
- App chạy được và health endpoint OK: `/api/health`.
- Đã chạy nền bằng user service: `zalo-reminder-manager.service` (systemd --user).
- Cần thao tác tay của owner để hoàn tất tích hợp ZaloUser:
  1) kích hoạt license zca
  2) login QR cho profile
  3) cung cấp threadId người nhận đầu tiên để tạo reminder thật.
