# 2026-02-26

- User said to call them "Boss".
- User named the assistant "Linh".
- User defines assistant role: family AI assistant, communicates in Vietnamese, polite, clear, and safety-first.
- User requested assistant pronoun: "em".
- User chose assistant signature emoji: 🌿.
- User requested: maintain a full system-change log in CongCu.md and push docs/config updates to GitHub after system-affecting changes.
- User requested improved elderly accessibility: better voice handling and easy-to-read responses.
- User requires mandatory `AllSetUp.md` changelog + immediate commit/push after every system-affecting setup/config change.
- User provided GitHub repo URL for setup sync: https://github.com/tritruc/openclawsetup.git
- GitHub auth completed and repository sync is now active on origin/main.
- User requested auto-run OpenClaw on startup and ability to operate local machine via chat (e.g., open YouTube karaoke).
- Implemented desktop action helpers: scripts/open_url.sh and scripts/open_youtube_search.sh.
- Added Windows Startup bridge file to start OpenClaw gateway automatically at login.
- Owner confirmed Chrome profile `AutomatedAccount` is pre-logged for Gmail/Facebook; set this as default account-action profile.
- Owner wants execution-first behavior: bot should complete tasks directly (not return tutorial-style instructions).
- Owner requested aggressive completion mindset (finish tasks with any practical method), with full logging in GitHub.
- Added hierarchical task structure under `ops/platforms/<platform>/<function>/<task-file>` and custom workspace skill `family-exec`.
- Fixed Chrome launcher quoting bug that could open wrong tab (`0.0.0.4`) when targeting profile `Profile 4`.
- Owner requested trying Camoufox/other software to maximize full automation for task completion.
- Installed Camoufox (workspace venv) and AutoHotkey (Windows user) as additional automation engines.
- Added AutoHotkey login automation for Gmail/Facebook (runtime credentials only, not persisted in files).
